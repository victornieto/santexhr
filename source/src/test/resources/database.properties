#=============================================================================
#
#					database.properties
#
#=============================================================================
# properties used by hbm2ddl and sample data scripts


# this property is in hibernate.cfg.xml, but it seems like hbm2ddl needs it here also
hibernate.dialect=${hibernate.dialect}

hibernate.namingStrategyClassName=${hibernate.namingStrategyClassName}

hibernate.connection.driver_class=${jdbc.driverClassName}
hibernate.connection.url=${jdbc.url}
hibernate.connection.username=${jdbc.username}
hibernate.connection.password=${jdbc.password}